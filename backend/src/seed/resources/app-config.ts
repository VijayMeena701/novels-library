const definition = {
  "key": "app_config",
  "name": "App Configs",
  "description": "Control System Behaviour using Configs",
  "category": "administration",
  "actions": [
    "read",
    "update",
    "manage"
  ],
  "isEnabled": true,
  "isSystem": true
} as const;

export default definition;
