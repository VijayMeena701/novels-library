const definition = {
  "key": "settings",
  "name": "Settings",
  "description": "User settings.",
  "category": "settings",
  "actions": [
    "read",
    "update",
    "manage"
  ],
  "isEnabled": true,
  "isSystem": true
} as const;

export default definition;
