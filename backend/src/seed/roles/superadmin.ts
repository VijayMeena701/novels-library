const definition = {
  "key": "superadmin",
  "name": "Superadmin",
  "description": "Full wildcard access to all resources and operations.",
  "groupKeys": [],
  "isSuperuser": true,
  "isSystem": true,
  "isDefault": false
} as const;

export default definition;
